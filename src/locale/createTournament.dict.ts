import { createTournament as en } from "./en/createTournament";
import { createTournament as pl } from "./pl/createTournament";

const createTournamentDict = {
  en: en,
  pl: pl,
};

export default createTournamentDict;
