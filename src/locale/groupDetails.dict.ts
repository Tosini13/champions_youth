import { groupDetails as en } from "./en/groupDetails";
import { groupDetails as pl } from "./pl/groupDetails";

const groupDetailsDict = {
  en: en,
  pl: pl,
};

export default groupDetailsDict;
