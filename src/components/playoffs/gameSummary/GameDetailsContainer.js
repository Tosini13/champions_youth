import React from 'react';
import GameDetails from './GameDetails';

const GameDetailsContainer = (props) => {
    return (
        <GameDetails {...props} />
    );
}

export default GameDetailsContainer;