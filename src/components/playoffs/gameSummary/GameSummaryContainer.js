import React from 'react';
import GameSummary from "./GameSummary";

const GameSummaryContainer = (props) => {
    return (
        <GameSummary {...props} />
    );
}

export default GameSummaryContainer;